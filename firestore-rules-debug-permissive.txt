// 🔒 REGLAS DE FIRESTORE TEMPORALES PARA DEBUG - DELIVERY USERS
// ⚠️ ESTAS REGLAS SON MUY PERMISIVAS - SOLO PARA TESTING
// Versión DEBUG para resolver problemas de permisos
// Copia estas reglas TEMPORALMENTE en Firebase Console → Firestore Database → Rules

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ✅ FUNCIONES AUXILIARES DE SEGURIDAD
    function isAuthenticated() {
      return request.auth != null;
    }

    function isAdmin() {
      return isAuthenticated() && (
        request.auth.uid == "byRByEqdFOYxXOmUu9clvujvIUg1" ||
        (request.auth.token.email != null && (
          request.auth.token.email == "hectorcobea03@gmail.com" ||
          request.auth.token.email == "hwcobena@espol.edu.ec"
        ))
      );
    }

    // 🆕 DELIVERY USERS: REGLAS MUY PERMISIVAS PARA DEBUG
    match /deliveryUsers/{userEmail} {
      // Permitir todo para usuarios autenticados (TEMPORAL)
      allow read, write, create, update, delete, list: if isAuthenticated();
    }

    // ✅ USUARIOS: Permisivo para testing
    match /users/{userId} {
      allow read, write, create, update: if isAuthenticated();
      allow delete: if false;
    }

    // ✅ INVENTARIO: Permisivo
    match /inventory/{productId} {
      allow read, list: if true;
      allow create, update, delete: if isAuthenticated();
    }

    // ✅ PRODUCTOS: Permisivo
    match /products/{productId} {
      allow read, list: if true;
      allow create, update, delete: if isAuthenticated();
    }

    // ✅ DELIVERY ORDERS: Permisivo
    match /deliveryOrders/{orderId} {
      allow read, write, create, update, list: if isAuthenticated();
      allow delete: if false;
    }

    // ✅ DELIVERY NOTIFICATIONS: Permisivo
    match /deliveryNotifications/{notificationId} {
      allow read, write, create, update, delete, list: if isAuthenticated();
    }

    // ✅ PEDIDOS DIARIOS: Permisivo
    match /dailyOrders/{date} {
      allow read, write, create, update, list: if isAuthenticated();
      allow delete: if false;
    }

    // ✅ CARRITO: Permisivo
    match /carts/{userId} {
      allow read, write, create, update, delete, list: if isAuthenticated();
      
      match /items/{itemId} {
        allow read, write, create, update, delete, list: if isAuthenticated();
      }
    }

    // ✅ RESTO DE COLECCIONES: Permisivo para testing
    match /{document=**} {
      allow read, write: if isAuthenticated();
    }
  }
}
